# Production environment variables for Vite build on Vercel.
# Vercel auto-loads this file during production builds.
# The /api path is relative — since the frontend and serverless function
# are served from the same Vercel deployment domain, no absolute URL is needed.
VITE_API_URL=/api
